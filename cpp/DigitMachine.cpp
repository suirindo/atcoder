#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

signed main(){
    vector<ll>a(10);
    for (int i = 0; i <= 9; i++){
        cin >> a[i];
    }

    ll num = 0;

    num = a[num];
    num = a[num];
    num = a[num];

    cout << num << endl;

    return 0;
}
