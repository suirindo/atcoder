#include <bits/stdc++.h>
using namespace std;

int main(){
    string str = "";
    cin >> str;
    
    for(int i = 0; i < (int)str.size(); ++i){
        char ch = str[i];
        string ans = str.sort(str[i], str[i + 1]);

}



}
